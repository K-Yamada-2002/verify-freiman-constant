# 2026-09-24: 反例を保存して修復後の子を展開

**全帰納は未完成。** 前回修復した指数0の根から再開した。
根の区間を狭めず、新しい子に局所規則を付け、失敗した枝の反例と親を調べた。
有限深さで隙間が見つからないことを、充填証明には使わない。

## 続行した部分戦略

| 状態 | 型数 | 局所規則 | 未解決 | 根から未解決までの最短距離 (0, 正) |
|---|---:|---:|---:|---:|
| 前回の根修復・要求区間調整後 | 2963 | 1088 | 1875 | 1, 4 |
| 修復後の子を展開 | 3023 | 1112 | 1911 | 4, 4 |
| 既知の反例を保持して指数0側を続行 | 3153 | 1156 | 1997 | 4, 4 |
| 別の後続で親を修復し、新しい子を展開 | 3155 | 1157 | 1998 | 4, 4 |

最初の続行は34候補中24規則、次の続行は47候補中44規則を受理した。
未解決数の増加は子の義務を残した結果で、解決率とは扱わない。
すべての保存規則を独立監査し、失敗0。
閉じた部分系は依然0で、`closed()` は未解決型を報告する。

- `repaired_frontier_round2.{json,state.json,audit.json,report.json}`
- `zero_frontier_with_gaps.{json,state.json,audit.json,report.json}`

## 新しい反例と有限深さの検査

指数0側の浅い未解決12型を深さ4・5・6・7で調べた。
型2984だけに深さ5の厳密な隙間を検出した。他の11型は隙間未検出。
2984への経路は `0 → 2960 → 2963 → 2969 → 2984`。
祖先2969と2963では深さ7まで隙間未検出だった。
このため、まず直前の親2969の被覆選択を変更する。

別の未解決型3008では深さ7まで隙間未検出。
こちらは反例があるとは判断せず、追加語長を2から3へ広げて探索する。
これらの節点番号は `repaired_frontier_round2.json` に対するもの。

指数0の根そのものも深さ6・7・8で検査し、隙間は検出しなかった。
根の充填証明や、さらに深い隙間の不存在を主張する結果ではない。

- `zero_round2_gap_probes.json`
- `zero_round2_ancestor_probes.json`
- `zero_round2_second_gap.json`
- `zero_root_deep_probe_round2.json`

## 反例による候補除外の修正と保存

前回の候補除外には座標の混同があった。
反例の区間端点は変換後のセルの正規化で表されるが、候補点の評価に
基底チャート上のアフィン正規化を返す `ChartSearch.value` を使っていた。
同じ幾何を表す場合も、これらの数値を直接比較してはいけない。

反例と同じ正規化で、数体の厳密値を評価するよう修正した。
変換を含む実例で、浮動小数にも `ChartSearch.value` にも依存しない試験を追加した。
修正後に前回の根修復を再実行し、証明書ハッシュ
`6556cdc77387350eaddee008a8abeb1e46e2c5c97bcf396c0188dd906c826329`
が一致した。前回保存した局所規則と監査結果は引き続き有効である。
ただし前回の候補除外が意図した通りだったという説明は、この訂正で置き換える。

もう一つ、同じ区間の基準点を変更すると反例の照合をすり抜ける問題も実データで確認した。
最初の親2969の修復候補は、子の `high` を変えるだけで同じ偽の区間を残していた。
`zero_round2_false_repair_audit.json` はその子3152の再反証である。
左右交換と基準点変更に伴う正規化倍率を数体で計算し、同じ反例を4表示に移すよう修正した。
左右交換が負の向きを持つ場合には、隙間の端点の順序も反転する。

修正後の `zero_round2_invariant_repair.json` は、親2969の目標区間を変えず、
`U,V2` の枝を別の合法な表示 `U2,V` に置き換えた。
もう一方の `U,V1` の枝と合わせて親を被覆する。
3153型・1156局所規則・1997未解決で、独立監査失敗0。
新しい2子に既知の7反例が当たらないことを、正規化変更と左右交換も含めて厳密に確認した。
その2子では深さ4・5・6・7の新しい隙間も検出しなかった。
これは子の充填証明ではない。

- `zero_round2_invariant_child_audit.json`：既知の反例との厳密な照合。
- `zero_round2_repaired_children_probes.json`：新しい2子の有限深さの探索。
- `zero_round2_invariant_repair.state.json`：7反例を保存した修復後の再開状態。

反例未検出の型3008は、追加語長3・端点候補上限256でも局所規則を得られなかった。
`zero_round2_longer_step.json` は変更なしの別実験であり、修復成功には数えない。

型3008への深さ8・9の追加検査は、5分超・約1.2GBのメモリ使用となったため停止した。
この実行は完了しておらず、どちらの深さについても「隙間なし」と判定していない。
完了済みの深さ7までの結果とは区別する。

さらに修復で得た新しい子にも局所規則を付けた。
最新状態は `zero_round2_repaired_expansion.state.json`。
3155型・1157規則・1998未解決で、根から未解決までの最短距離は両族とも4。
閉じた部分系は0。反例を1個修復しても帰納全体が閉じるわけではない。

既知の6反例を、型番号に依存しない点領域の証明書と排除木として保存した。
再開時に排除木を厳密に再検証し、相関チャートへの点の包含を逆変換で確認する。
同じ禁止語状態・向きでも、領域に入らない反例は適用しない。
区間端点の候補には隙間の左右を表す別の符号を付け、その隙間をまたぐ候補を除く。
この除外は発見側のみ。採用する局所規則は従来の検証器で独立に検証する。

`gap_filter_bank` を再開状態に保存し、`search_root_frontier.py` と
`specialize_piecewise_search.py` で引き継ぐ。
前回の「再開すると反例の候補除外を失う」制約を、この2経路では解消した。
他の探索入口はこの拡張フィールドを読むよう変更していないため、再開には下記を使う。

## 再開

```sh
python3 search_root_frontier.py zero_round2_repaired_expansion.state.json \
  --focus-root zero --seconds 180 --attempts 60 --max-step 2 \
  --max-shapes 256 --partition-depth 4 --chart-memory 3 \
  --native-executable /tmp/freiman-chart-planner-20260924 --output next_zero.json
```

`--focus-root` は処理する枝の優先範囲のみを変える。
正の指数の根とすべての依存は証明書・最終監査に残す。
前回の型2984は親の別表示で回避した。節点番号は剪定・再登録で変わるため、
型を照合するときは領域・要求区間を確認し、古い番号をそのまま流用しない。
全列挙の再開位置は従来の `hybrid_induction_20260924_finish.state.json` に保存したまま。
この限定した発見側だけに、相対的完全性は主張しない。

## 検査

全168試験が通過。その後、基準点変更・左右交換後の反例を独立検証器で
再検証する試験を1件追加し、それを含む関連10試験も通過した。
点領域の正規化、相関領域への包含、反例のJSON往復と偽造拒否、
片方の根を優先しても他方を削除しないことも検査した。
一覧は `gap_bank_continuation_summary.json`。
